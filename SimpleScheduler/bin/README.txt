Simple Scheduler Algorithm

The program does not run. I do not understand how locks work
and was unable to figure out how to pass the lock from each thread in order
to block when necessary and ensure that only one thread executed at a time.